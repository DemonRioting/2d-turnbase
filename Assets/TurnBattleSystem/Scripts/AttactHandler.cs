using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttactHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void selectCharacterAttact(CharacterBattle targetCharacterBattle, Action onAttackComplete, int min, int max, int typeOfCharacter) {
        if (typeOfCharacter == 1) {

        } else if (typeOfCharacter == 2) {

        } else if (typeOfCharacter == 3) {
            
        } else if (typeOfCharacter == 4) {
            
        } else {
            
        }
    }
}
